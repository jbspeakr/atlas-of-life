# Decisions

## Stack and scope

Vite 7, React 19, TypeScript 5, MapLibre GL JS 5.24 or newer within major 5, PMTiles 4, Zod, Vitest and Playwright are resolved to exact installed versions in the lockfile. MapLibre circle and symbol layers provide the marks, glow and collision handling without deck.gl: a second rendering engine does not earn its dependency and interoperation cost here. All authored information is geographic, dates or non-personal tags. City publication precision is the default; explicit landmark overrides are intentional public disclosures.

## Art direction

The palette is midnight `#080f18` (ocean), basalt `#121c27` (land), contour `#2b3947` (linework), lamplight `#efc784` (the single warm accent), parchment `#f1eee7` (primary text), and mist `#a7b2bf` (secondary text). Fraunces supplies distinctive, softly sculpted place names and the opening title; Noto Sans supplies quiet controls and map labels. Both are self-hosted WOFF2 with their licence files. The original Inter UI choice was deliberately replaced before final approval: retaining Noto map glyphs alongside Inter and Fraunces would have violated the two-family limit. The map fills the viewport; an editorial title sits upper left, a restrained chronological index lower left, a thin year control across the foot, and navigation upper right. The selected place is a typographic caption, never a story card. No photographs, personal names, notes, decorative gradients, or unrelated content are supported.

## Motion

The only autonomous entrance is the globe drawing in and countries lighting in first-visit order. Deterministic mode and reduced motion disable this and idle rotation. Zoom-dependent paint expressions keep all semantic layers present, overlapping continuously. The timeline remains because dates are core geographic metadata; it changes feature-state, never source identity. Navigation uses bounding boxes and a weighted cubic easing; the globe and compass are always recoverable.

## Verification contract

The verification harness precedes application code. Missing outputs, assets, runtime interfaces and visual baselines are failures, not skips. Baselines are approved only by the explicit approval command. Runtime instrumentation is exposed through `window.__atlas` for a fixed production-browser choreography; the normal application does not fetch any verification resources. Full verification serves the build under `/atlas/`, at a pinned viewport and DPR, and records objective metrics including instability rather than hiding it with retries.

## Boundary and publication architecture

Build-time processing owns all sensitive fields. Runtime places have an explicit allowlist and never import the authored config or geocode cache. Countries use Natural Earth's `ISO_A2_EH`; ADM1 uses per-country geoBoundaries gbOpen with documented Natural Earth fallback. Download manifests pin bytes by SHA-256. Offline normal builds use committed fixture boundaries and warm geocodes; source refresh is explicit. Two weighted Visvalingam LODs and interior label anchors keep geometry compact and navigation reliable.

## Hosting status

The preferred production host is a **Hugging Face dataset resolve URL pinned to a full commit SHA**. A live 6.5 GB, LFS/Xet-backed public archive passed the exact 16,384-byte Range request, redirect-target CORS, OPTIONS and real Chromium rendering checks. This establishes large-object browser transport, not permission to publish into someone else's dataset; the probe sample is an MGRS layer, not the application's OSM basemap. No unrelated third-party archive is wired in as the default basemap. Without a configured remote URL the repository runs its local example; explicit `VITE_BASEMAP=remote` requires a URL and rejects mutable HF revisions and dated planet build URLs. This out-of-box preview choice avoids inventing a stable public OSM endpoint; the owner must publish the supplied full extract or choose their own verified URL for production remote mode.

### Measured hosting evidence

Evidence is preserved in `verification/hosting-evidence.json`; live reruns append to ignored `verification/hosting-results.json`. All remote probes originate from an actual Chromium page at `http://127.0.0.1:4178`, not curl-only assertions.

| Candidate                | Measured endpoint/result                                                                                                                                                                                                                                                                                     | Decision                                                                                                                                                                           |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HF dataset               | `smartmaps/mgrs-pmtiles`, SHA `f8d8eb59c3e3d34d69aaea8acdc304c92e5f6e32`, `mgrs-hokkaido.pmtiles`: 6,515,998,388 bytes; HEAD 200 + ranges; resolve 302 → `us.aws.cdn.hf.co` 206; bytes 0–16383 returned exactly; CORS `*`, exposed Content-Range, preflight and real tiles pass; zero console errors         | Preferred **transport**. The current application-compatibility probe correctly fails this sample's regional bounds and non-Protomaps schema. It is not an application basemap URL. |
| HF Storage Bucket        | Public `julien-c/my-training-bucket/resolve/pi-logo-dark.svg` was discoverable without credentials and served cross-origin ranged bytes, disproving a blanket “all public access needs S3 credentials” assumption. It is a 473-byte SVG, not a PMTiles archive, so archive parsing/rendering correctly fails | **Not certified for this app.** No real public bucket PMTiles URL or owner credentials were supplied; do not infer large archive compatibility from the SVG control.               |
| Cloudflare R2            | Official `r2-public.protomaps.com/protomaps-sample-datasets/cb_2018_us_zcta510_500k.pmtiles`: 20,926,494 bytes; exact 16 KB range and browser rendering pass; CORS exposes ETag but **not Content-Range**                                                                                                    | The tested endpoint fails the strict header gate. R2 remains an option with an owner-controlled CORS rule exposing both headers; that rule was not silently assumed.               |
| Same-origin static files | The complete 211,759,571-byte production extract passes exact 16 KB Range, header min/max/global bounds, required Protomaps layers and real browser rendering. The committed 22,331,674-byte fixture was also probed separately                                                                              | Fully verified local/self-contained transport.                                                                                                                                     |

The publishing script's unauthenticated path was exercised: it fails before uploading or modifying `.env.production`. A real upload, final immutable OSM resolve URL, owner R2 policy and owner HF Storage archive remain external prerequisites. The probes never manufacture resources or upload geographic information without the owner's configured account. Where headers are controlled, `_headers` sets immutable archive/hashed-asset caching and exposed range metadata; an immutable URL is required when replacing archive bytes.

The hosting command now defaults to application expectations: vector MVT, z0–14, global bounds, and Protomaps `earth`, `water`, and `roads` layers. It does not silently treat unspecified expectations as passing. Earlier generic transport probes are retained as history; their success is not an app-compatibility certificate. The R2 sample also fails application coverage/schema in addition to its exposed-header defect. A synthetic cross-loopback probe hit Chromium's local-network protection; actual statically served pages on separate loopback origins passed, so that synthetic-page failure is not misreported as a range/CORS defect in the archive.

The full archive was then loaded in two real production builds under `/atlas/`: bundled at port 4180 and remote at port 4181 fetching from port 4179. Both rendered actual `earth` features, selected Berlin by clicking the map, restored focus with Escape and restored deep links. All console-error counts were zero, origins were exactly the declared app/tile origins, and globe plus city screenshots matched at **zero pixels**. Query-tag filtering was also exercised: an `art` filter extinguished London's pin (red channel 29) while Paris remained lit (239), and the “Show all” link cleared the filter.

### Archive extraction and provenance

The complete bundled path was run against the source discovered at `https://maps.protomaps.com/builds`: `https://build.protomaps.com/20260914.pmtiles`, gzip MVT, global z0–15. The actual result is **211,759,571 bytes**, global z0–6 and six 20 km city windows through z14, below the 250 MB warning threshold. Current `pmtiles extract` writes PMTiles, not MBTiles, so the script explicitly converts extracts with `tile-join`, materializes a flat SQLite union of whole tile coordinates, and converts the merged MBTiles back to PMTiles. This avoids duplicate features where city windows overlap and handles Tippecanoe's normalized `tiles` view. `pmtiles verify` passed. The production archive is ignored rather than committing a >100 MB file to GitHub; the immutable 22.3 MB fixture is committed independently and always staged for verification.

### Geometry, typography and preview refinements

Natural Earth's full country bbox includes overseas territories; the interior anchor uses the largest polygon instead of an offshore centroid. `XK` is accepted as the explicitly documented Natural Earth Kosovo identifier, although it is not an assigned ISO alpha-2 code; upstream region lookup uses `XKX`. Country/region-only visits work without a geocode. A coarse earth underlay, reading lower zoom tiles from the **same archive**, keeps land continuous outside high-detail extracts. All non-visited basemap colours are desaturated blue-grey rather than inheriting warm road colours from the stock theme. The mobile globe uses a smaller camera scale so it fits 375px instead of cropping into a continent view. Additional glyph ranges are fetched at build time only from the pinned asset revision, then self-hosted; fixtures forbid missing-range downloads.

## Maintainer and accessibility choices

Tags are usable through shareable `?tag=coast`-style URLs, with a quiet status/clear link only when active, rather than a permanent row of filter chips. They combine with the year scrubber through feature-state visibility and retain the one-accent palette rather than introducing categorical hues. There is no deck.gl or terrain dependency: native MapLibre circles, text collision placement and a hovered halo cover the requested effects without another rendering stack. Repeated-visit aliases replace their canonical hash instead of pushing another history entry, so browser Back cannot become trapped.

## CI and measurement environment

Full verification uses a real GPU and exact platform-specific PNGs, so the workflow targets a dedicated `macOS`, `ARM64`, `atlas-gpu` runner. It runs on every repository push and trusted same-repository pull request; untrusted fork code is not automatically executed on a persistent self-hosted machine. Review and mirror fork work onto a trusted branch, and provision an isolated runner without personal credentials. The commented Cloudflare job rebuilds with `/` instead of incorrectly reusing GitHub Pages' repository subpath. Both deployment providers still require the owner's account permissions.

## Correcting the instability reporting contract

The first harness implementation incorrectly overrode a numerically passing p95 check with `fail` whenever its five-run spread exceeded 20%. The brief explicitly says to **gate on the median and flag instability**, not to replace the 24 ms gate with a stability gate. One measured run had median 21.528 ms and spread 5.16 ms: it should report `pass`, `unstable: true`, not claim 21.528 exceeded 24. This correction is isolated in its own commit with this explanation. The 24 ms p95 budget, 32 ms long-frame definition, long-frame limit, all five samples and the 20% instability flag remain unchanged. Flagged results remain visible evidence and are not treated as proof of a stable performance improvement; no retries or quarantine are used to force green.
